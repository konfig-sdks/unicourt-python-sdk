# coding: utf-8

"""
    UniCourt Enterprise APIs

    <button><a href=\"/enterpriseapi/download/UniCourt-Enterprise-API-Spec.yaml\" >Download UniCourt Enterprise APIs Specification</a></button> 

    The version of the OpenAPI document: 1.0.0
    Generated by: https://konfigthis.com
"""

from datetime import datetime, date
import typing
from enum import Enum
from typing_extensions import TypedDict, Literal, TYPE_CHECKING
from pydantic import BaseModel, Field, RootModel

from unicourt_python_sdk.pydantic.docket_entry_primary_documents import DocketEntryPrimaryDocuments
from unicourt_python_sdk.pydantic.docket_entry_secondary_documents import DocketEntrySecondaryDocuments
from unicourt_python_sdk.pydantic.referenced_docket_number import ReferencedDocketNumber
from unicourt_python_sdk.pydantic.source_structured_data import SourceStructuredData

class DocketEntry(BaseModel):
    # Determines if it is the first docket entry or the last docket entry. This value will be set only for the first and last docket entry. For other docket entries it will be null. However, this will be set as single_docket_entry when the Case contains only one docket entry.
    boundary: Literal["first", "last", "single_docket_entry", None] = Field(alias='boundary')

    # Docket Badge helps you to know what type of a docket entry it is.
    docket_badge: Literal["DOCKET", "CHARGE", "HEARING", "JUDGEMENT", "FINANCIAL", "DISPOSITION", "SENTENCE", "BOND", "BAIL", "CAUSE_OF_ACTION", "ARREST", "WARRANT", "SUMMON", "TICKLER", "OPINION"] = Field(alias='docketBadge')

    # Docket Action Date
    docket_entry_date: datetime = Field(alias='docketEntryDate')

    docket_entry_primary_documents: DocketEntryPrimaryDocuments = Field(alias='docketEntryPrimaryDocuments')

    docket_entry_secondary_documents: DocketEntrySecondaryDocuments = Field(alias='docketEntrySecondaryDocuments')

    # The respective docket entry number which is defined in the court website.
    docket_number: typing.Optional[int] = Field(alias='docketNumber')

    # When this docket entry was last fetched from the source.
    last_fetch_date: str = Field(alias='lastFetchDate')

    # Name of the object
    object: str = Field(alias='object')

    # Other Docket Numbers that referenced for a particular docket entry.
    referenced_docket_number_array: typing.List[ReferencedDocketNumber] = Field(alias='referencedDocketNumberArray')

    # Order number how the dockets have stored in UniCourt.
    sort_order: int = Field(alias='sortOrder')

    # Source Docket Entry
    text: str = Field(alias='text')

    text_structured: SourceStructuredData = Field(alias='textStructured')
    class Config:
        arbitrary_types_allowed = True
